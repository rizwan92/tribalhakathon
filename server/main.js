import { Meteor } from 'meteor/meteor';
import '../imports/api/district';
import '../imports/api/tsparea';
import '../imports/api/department';
import '../imports/api/scheme';
import '../imports/api/project';

Meteor.startup(() => {
  // code to run on server at startup
});
